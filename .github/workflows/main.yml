name: Ecosystem check

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "**"

permissions:
  contents: read

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  UV_VERSION: 0.1.23

jobs:
  build-and-compare:
    runs-on: ubuntu-24.04
    steps:
      - name: Create builds directory
        run: mkdir -p ./builds

      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Build PR version
        run: cargo build --release --verbose && mv target/release/djangofmt ./builds/pr_build

      - uses: actions/checkout@v4
        with:
          ref: main
          persist-credentials: false

      - name: Build main version
        run: cargo build --release --verbose && mv target/release/djangofmt ./builds/main_build

      - name: Setup uv
        uses: astral-sh/setup-uv@v5 # zizmor: ignore[cache-poisoning]

      - name: Run `djangofmt` ecosystem check
        run: |
          # Set pipefail to avoid hiding errors with tee
          set -eo pipefail

          uv venv --python 3.12
          uv pip install ./python/ecosystem-check
          ls -al
          uv run ecosystem-check format ./builds/main_build ./builds/pr_build --cache ./checkouts --output-format markdown | tee ecosystem-result-format-stable

          cat ecosystem-result-format-stable > "$GITHUB_STEP_SUMMARY"
          {
            echo "### Formatter (stable)"
            cat ecosystem-result-format-stable
            echo ""
          } >> ecosystem-result
